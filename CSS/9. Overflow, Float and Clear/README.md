# clear property (CSS)

## Purpose:
Prevents an element from sitting beside floating elements.

### Values:
clear: none   → default, can sit beside floats

clear: left   → move below left floats

clear: right  → move below right floats

clear: both   → move below all floats

# Rule:
Only the element with `clear` is affected.
Other elements can still sit beside floats.